{% extends '@layout/default.html.twig' %}

{% block title %}Register{% endblock %}
{% block page_title %}{% endblock %}

{% block body_class %}user-form{% endblock %}

{% block content %}
<div class="container">
        <div class="row">
            <div class="col-sm-12">
                <div class="register-block">
                    <h3>Register</h3>

                    {{ messagesPartial('partial::alerts', {}, null, 'user-register') }}

                    {{ form().openTag(form)|raw }}

                    {% set details = form.get('detail') %}

                    {{ formElement(details.get('firstName')) }}

                    {{ formElement(details.get('lastName')) }}
                    
                    {{ formElement(details.get('phoneNumber')) }}

                    {{ formElement(form.get('email')) }}

                    {{ formElement(form.get('password')) }}

                    {{ formElement(form.get('passwordConfirm')) }}

                    {{ formElement(form.get('userRegisterCsrf')) }}
                    
                    <div class='form-check'>
                    {{ formCheckbox(form.get('terms')) }}
					{{ formLabel(form.get('terms')) }}
					</div>
					
                    {{ formSubmit(form.get('submit')) }}

                    {{ form().closeTag()|raw }}

                    <hr>
                    <p class="terms">When you sign up for an account, you agree to our <a href="{{ path('page', {action: 'terms'}) }}">Terms of Service</a> and our <a href="{{ path('page', {action: 'privacy'}) }}">Privacy Policy</a>.</p>
                    <p>Already registered? <a href="{{ path('user', {action: 'login'}) }}">Log in</a>!</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
